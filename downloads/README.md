Déposez ici le fichier `syrix-flix.apk` (build Flutter release) : il sera
automatiquement servi sur `/downloads/syrix-flix.apk` par le backend, c'est
l'URL utilisée par la bannière `web/assets/js/apk-banner.js`.
